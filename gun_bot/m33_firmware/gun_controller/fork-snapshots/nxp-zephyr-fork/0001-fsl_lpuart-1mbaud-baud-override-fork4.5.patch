diff --git a/mcux/mcux-sdk-ng/drivers/lpuart/fsl_lpuart.c b/mcux/mcux-sdk-ng/drivers/lpuart/fsl_lpuart.c
index 0f196a639..f359eed06 100644
--- a/mcux/mcux-sdk-ng/drivers/lpuart/fsl_lpuart.c
+++ b/mcux/mcux-sdk-ng/drivers/lpuart/fsl_lpuart.c
@@ -432,6 +432,17 @@ status_t LPUART_Init(LPUART_Type *base, const lpuart_config_t *config, uint32_t
         }
     }
 
+    /* MobTurret workaround: SDK's baud search produces wrong SBR/OSR for
+     * 24 MHz + 1 Mbaud on iMX93 M33 (yields SBR=1028, OSR=0). Hardcode
+     * the correct values: 24 MHz / (24 * 1) = 1 MHz exactly.
+     * BAUD register = (OSR-1)<<24 | SBR = (23<<24) | 1 = 0x17000001. */
+    if (srcClock_Hz == 24000000U && config->baudRate_Bps == 1000000U)
+    {
+        osr      = 24U;
+        sbr      = 1U;
+        baudDiff = 0U;
+    }
+
     /* Check to see if actual baud rate is within 3% of desired baud rate
      * based on the best calculate OSR value */
     if (baudDiff > ((config->baudRate_Bps / 100U) * 3U))
@@ -836,6 +847,17 @@ status_t LPUART_SetBaudRate(LPUART_Type *base, uint32_t baudRate_Bps, uint32_t s
 
     /* Check to see if actual baud rate is within 3% of desired baud rate
      * based on the best calculate OSR value */
+    /* MobTurret workaround: SDK's baud search produces wrong SBR/OSR for
+     * 24 MHz + 1 Mbaud on iMX93 M33 (yields SBR=1028, OSR=0). Hardcode
+     * the correct values: 24 MHz / (24 * 1) = 1 MHz exactly.
+     * BAUD register = (OSR-1)<<24 | SBR = (23<<24) | 1 = 0x17000001. */
+    if (srcClock_Hz == 24000000U && baudRate_Bps == 1000000U)
+    {
+        osr      = 24U;
+        sbr      = 1U;
+        baudDiff = 0U;
+    }
+
     if (baudDiff < (uint32_t)((baudRate_Bps / 100U) * 3U))
     {
         /* Store CTRL before disable Tx and Rx */
